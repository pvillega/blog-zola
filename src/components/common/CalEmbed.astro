---
export interface Props {
  calLink?: string;
  eventType?: string;
  class?: string;
}

const {
  calLink = 'your-username/30min',
  eventType = '30min',
  class: className = '',
} = Astro.props;
---

<!-- Cal.com Embed Placeholder -->
<div class={`cal-embed-container ${className}`}>
  <div class="max-w-4xl mx-auto p-8 border-2 border-dashed border-gray-300 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-800">
    <div class="text-center space-y-4">
      <svg
        class="w-16 h-16 mx-auto text-gray-400 dark:text-gray-500"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
        ></path>
      </svg>
      <h3 class="text-2xl font-bold text-gray-700 dark:text-gray-300">Cal.com Booking Widget</h3>
      <div class="text-gray-600 dark:text-gray-400 space-y-2">
        <p class="text-lg font-medium">Configuration Needed</p>
        <p class="text-sm">Cal.com link: <code class="px-2 py-1 bg-gray-200 dark:bg-gray-700 rounded">{calLink}</code></p>
        <p class="text-sm">Event type: <code class="px-2 py-1 bg-gray-200 dark:bg-gray-700 rounded">{eventType}</code></p>
      </div>
      <div class="mt-6 p-4 bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded">
        <p class="text-sm text-blue-800 dark:text-blue-200 font-medium mb-2">To activate Cal.com integration:</p>
        <ol class="text-xs text-left text-blue-700 dark:text-blue-300 space-y-1 max-w-md mx-auto">
          <li>1. Create a free account at <a href="https://cal.com" target="_blank" rel="noopener" class="underline hover:text-blue-600">cal.com</a></li>
          <li>2. Set up your event types (e.g., "30 Minute Meeting")</li>
          <li>3. Get your embed code from Cal.com settings</li>
          <li>4. Replace the TODO section in this component with the Cal.com embed script</li>
          <li>5. Update the calLink prop to match your Cal.com username and event</li>
        </ol>
      </div>
    </div>
  </div>
</div>

<!--
  TODO: Replace this entire component with actual Cal.com embed code

  Example Cal.com embed integration:

  1. Add Cal.com inline embed script in the <head> or before </body>:
     <script type="text/javascript" src="https://app.cal.com/embed/embed.js"></script>

  2. Add the data-cal-link attribute to an element:
     <div
       data-cal-link="{calLink}"
       data-cal-config='{"layout":"month_view"}'
       style="width:100%;height:100%;overflow:scroll"
     ></div>

  3. Or use the Cal namespace method:
     <script>
       Cal("init", {origin:"https://app.cal.com"});
       Cal("inline", {
         elementOrSelector:"#cal-booking",
         calLink: "{calLink}",
         layout: "month_view"
       });
     </script>
     <div id="cal-booking" style="width:100%;height:100%;overflow:scroll"></div>

  For more details, see: https://cal.com/docs/integrations/embed
-->

<style>
  .cal-embed-container {
    min-height: 400px;
  }
</style>
